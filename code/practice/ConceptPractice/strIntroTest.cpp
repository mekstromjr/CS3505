#include <iostream>
#include <string>

using namespace std;
//Messing around with strings, a "compound data type" in c++, meaning it is assigned/resassigned
//  in a similar fasion to fundemental (primitive) types.cd
int main() {

    string s("abc");
    s = "def";


    return 0;
}